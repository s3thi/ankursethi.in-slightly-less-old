import Typography from "typography";

const typography = new Typography({
  baseFontSize: "18px",
  baseLineHeight: "1.666",
  bodyFontFamily: ["Inria Sans", "Helvetica Neue", "Segoe UI", "Helvetica", "Arial", "sans-serif"],
  headerFontFamily: ["Inria Serif", "Georgia", "serif"]
});

export default typography;
